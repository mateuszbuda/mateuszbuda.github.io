source 'https://rubygems.org'

gem "github-pages", group: :jekyll_plugins

# Required for `jekyll serve` on Ruby 3.x (webrick was removed from stdlib).
# Local-development only; ignored by GitHub Pages' build environment.
gem "webrick", "~> 1.8"

